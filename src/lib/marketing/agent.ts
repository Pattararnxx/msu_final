import { createDeepSeek } from "@ai-sdk/deepseek";
import { getMenuSnapshot, getSalesSnapshot } from "./tools";

export const MODEL_ID = "deepseek-chat";
export const MAX_TOOL_STEPS = 4;

export function hasApiKey(): boolean {
  return Boolean(process.env.DEEPSEEK_API_KEY);
}

export function getModel() {
  const deepseek = createDeepSeek({ apiKey: process.env.DEEPSEEK_API_KEY ?? "" });
  return deepseek(MODEL_ID);
}

export const SYSTEM_PROMPT = `คุณคือ "ผู้ช่วยการตลาดร้าน" ของร้านอาหารเช้าโจ๊ก ข้าวต้ม และก๋วยจั๊บ
คุณช่วยเจ้าของร้านหรือพนักงานคิดแผนการตลาดที่ทำได้จริงในช่วงเวลางาน เช่น เลือกเมนูที่จะดัน คิดชุดอาหารเช้า ร่างข้อความโพสต์ และวางแผนโปรโมชันรายวัน
ตอบภาษาไทยเป็นหลัก กระชับ เป็นขั้นตอน และใช้ภาษาที่คนหน้าร้านเข้าใจ

กฎข้อมูล:
1. ก่อนพูดถึงชื่อเมนู ราคา เมนูขายดี ยอดขาย หรือหมวดที่ควรโปรโมต ต้องเรียก tool ที่เกี่ยวข้องในเทิร์นนี้ก่อนเสมอ
2. ราคาต้องมาจาก getRestaurantMenu หรือผลลัพธ์ calculateBundlePrice เท่านั้น ห้ามเดาหรือสร้างราคาใหม่เอง
3. คำว่า "ขายดี" ต้องอ้างจาก getSalesInsights และบอกว่าเป็นข้อมูลตัวอย่าง/ข้อมูล ณ วันที่ tool คืนมา ไม่ใช่ยอดสด ถ้า tool ระบุเช่นนั้น
4. ไอเดียส่วนลด ชุดเมนู และข้อความโพสต์เป็น "ร่างข้อเสนอ" ต้องให้เจ้าของร้านอนุมัติก่อนใช้จริง ห้ามอ้างว่าจะเพิ่มยอดกี่เปอร์เซ็นต์ถ้าไม่มีข้อมูลรองรับ
5. ห้าม auto-publish, ยิงแอด, ส่งข้อความหาลูกค้า หรือเปลี่ยนราคา/เมนูในระบบ คุณทำได้แค่ให้คำแนะนำและร่างเนื้อหา
6. ถ้าผู้ใช้ถามเรื่องใบออร์เดอร์ OCR ให้เตือนว่าผล AI เป็นคำแนะนำ รายการที่ความมั่นใจต่ำต้องมี human review ก่อนคิดเงินหรือเตรียมครัว
7. ถ้าไม่มีข้อมูลพอ ให้บอกสิ่งที่ยังไม่รู้และถามคำถามถัดไปที่จำเป็น ห้ามแต่งข้อมูลแทน
8. ใช้ Markdown แบบง่ายได้: หัวข้อสั้นๆ และรายการทีละบรรทัด ไม่ใช้ตารางกว้างๆ และไม่ใช้ emoji
9. คำตอบปกติไม่เกิน 10 บรรทัด เริ่มด้วยข้อสรุปที่ทำต่อได้ทันที แล้วค่อยใส่เหตุผลหรือขั้นตอน

แนวทางการทำงาน:
- ถ้าผู้ใช้ขอโปรโมตเมนูหรือเขียนโพสต์แบบเจาะจง ให้เรียก getRestaurantMenu และถ้าต้องเลือกจากความนิยมให้เรียก getSalesInsights ด้วย
- ถ้าผู้ใช้ขอชุดเมนู/ราคาโปร ให้เรียก getRestaurantMenu แล้วใช้ calculateBundlePrice โดยเสนอเป็นร่างเท่านั้น
- ถ้าผู้ใช้ถามว่าเริ่มทำการตลาดอะไรวันนี้ ให้เรียก getSalesInsights และเสนอ 1 แผนหลัก + 1 ทางเลือกที่ทำได้จริงในร้าน
- เน้นช่องทางที่ร้านทำเองได้ เช่น ป้ายหน้าร้าน, Facebook, LINE, เดลิเวอรี่ และโพสต์ช่วงเช้า แต่ห้ามอ้างผลลัพธ์ที่ยังไม่มีข้อมูลจริง`;

function findMentionedMenu(userText: string) {
  return getMenuSnapshot().find((item) => userText.includes(item.name));
}

export function buildMockAnswer(userText: string): string {
  const mentionedMenu = findMentionedMenu(userText);
  const topItems = getSalesSnapshot().slice(0, 3);

  if (mentionedMenu) {
    return [
      "[โหมดออฟไลน์] ร่างแนวทางโปรโมตเมนู",
      `- ${mentionedMenu.name} ราคาใน catalogue ${mentionedMenu.price.toLocaleString("th-TH")} บาท`,
      `- ข้อความโพสต์: “เช้านี้ลอง${mentionedMenu.name} ร้อนๆ ทานง่าย อิ่มพร้อมเริ่มวัน”`,
      "- ช่องทางแนะนำ: โพสต์หน้าร้านและ Facebook ก่อนช่วง 06:00–08:00",
      "- ตรวจราคาและข้อความให้ตรงกับโปรที่ร้านอนุมัติก่อนเผยแพร่",
    ].join("\n");
  }

  if (/ขายดี|ยอดขาย|โปรโมต|โปร|การตลาด|วันนี้/.test(userText)) {
    return [
      "[โหมดออฟไลน์] จากยอดตัวอย่างในระบบ เมนูที่ถูกสั่งมากสุดคือ:",
      ...topItems.map((item, index) => `${index + 1}. ${item.name} ${item.quantitySold} ที่`),
      "",
      "ข้อเสนอวันนี้: ใช้เมนูอันดับ 1 เป็นตัวนำ แล้วจับคู่เครื่องดื่มหรือของทานเล่นเป็นชุดร่าง",
      "หมายเหตุ: ตัวเลขเป็นข้อมูลตัวอย่าง ไม่ใช่ยอดขายสด และโปรต้องให้เจ้าของร้านอนุมัติก่อนใช้จริง",
    ].join("\n");
  }

  return [
    "[โหมดออฟไลน์] ฉันช่วยวางแผนการตลาดร้านได้ เช่น",
    "- เลือกเมนูขายดีจากข้อมูลในระบบ",
    "- คิดชุดอาหารเช้าและคำนวณราคาโปรแบบร่าง",
    "- ร่างข้อความโพสต์สำหรับ Facebook หรือ LINE",
    "ลองพิมพ์ว่า “ช่วยคิดโปรโมชันจากเมนูขายดี” ได้เลย",
  ].join("\n");
}
